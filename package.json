{
  "name": "bookstore-mongodb",
  "version": "1.0.0",
  "description": "Ứng dụng quản lý cửa hàng sách sử dụng MongoDB",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "seed": "node seed-data.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^4.18.2",
    "mongoose": "^7.5.0"
  }
} 